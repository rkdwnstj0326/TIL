# 이 파일을 복사해서 .env 로 저장하고, 실제 값을 채워넣으세요.
#   cp .env.example .env
#
# .env 파일은 절대 커밋하지 않습니다 (아래 .gitignore 참고).

OPENAI_API_KEY=
# 생략 시 기본값(gpt-5-nano) 사용 — 필요할 때만 지정
OPENAI_MODEL=gpt-5-nano

# 학생 개인에게 발급된 SSAFY GMS 키
# GMS_KEY=여기에_GMS_KEY_입력
# GMS_BASE_URL=

