'''
문제 04 (problem04.py)
------------------------------------------------------------------------
매장의 주간 실적을 정리하고 있습니다.
1주차부터 순서대로 기록된 주별 순이익이 리스트 profits로 주어질 때,
그 주까지의 역대 최고 순이익을 새로 경신한 주의 인덱스(0부터 시작)를
모두 모아 리스트로 반환하는 find_record_weeks 함수를 완성하시오.

- 1주차는 이전 기록이 없으므로 언제나 경신한 주로 봅니다.
- 이전 최고 기록과 순이익이 같은 주는 경신한 것이 아닙니다.
- 순이익은 적자일 수 있어 음수가 들어올 수 있습니다.
- 실적 리스트에는 최소 1개 이상의 정수가 담겨 있습니다.

예시_01)
    입력: [10, 30, 20, 30, 45]
    출력: [0, 1, 4]
          (0주차 10은 첫 기록 / 1주차 30은 10 경신 / 3주차 30은 동점이라 제외
           / 4주차 45는 30 경신)

예시_02)
    입력: [5]
    출력: [0]

예시_03)
    입력: [9, 8, 7]
    출력: [0]           (첫 주 이후로는 한 번도 경신하지 못함)

예시_04)
    입력: [-10, -20, -5]
    출력: [0, 2]        (-5는 -10보다 크므로 경신)

제한 기능: max, 슬라이싱(profits[:i] 같은 표기),
           리스트 컴프리헨션([... for ... in ...])
           => 사용해도 됩니다. 다만 기본적으로 사용하지 말고 구현해보세요.
'''

# ============================================================
# [1] 신호어 찾기  (코드를 치기 전에 작성)
# ============================================================
# 지문에서 찾은 신호어 : 역대 최고 순이익을 새로 경신한 주의 인덱스, 모두모아, 리스트로 반환
# 선택한 골격         : (이 문제는 골격이 2개입니다. 모두 쓰세요) 누적, 탐색
# 그렇게 판단한 이유   : 역대 최고 순 이익을 계속 비교해야하고, 그리고 모두 모으라고 해서 리스트에 누적해서 반환해야함
#
# 반환 형태 (지문에서 그대로 옮겨 적기) : 역대 최고 순이익을 새로 경신한 주의 인덱스(0부터 시작)를 모두 모아 리스트로 반환
#
# 추가 질문: 03번은 '전체에서 가장 높은 값'을 기준으로 골라냈고,
#            이 문제는 '그 시점까지 가장 높은 값'을 기준으로 골라냅니다.
#            이 차이 때문에 03번에는 있었지만 여기에는 없어도 되는 동작이
#            하나 있습니다. 무엇일까요?
# 답 : 4번은 신기록을 전부 남겨야 한다?


# ============================================================
# [2] 변수 역할표  (코드를 치기 전에 작성)
# ------------------------------------------------------------
# 만들 변수마다 아래 네 칸을 채우세요.
# 이름은 '담기는 것'과 반드시 일치해야 합니다.
# '순이익 값'이 담기는 변수와 '인덱스'가 담기는 변수의 이름을 구분하세요.
# ============================================================
# 변수 이름 | 담기는 것 | 시작값 | 그 시작값이어야 하는 이유
#  high   |역대 최고 순이익| profits[0] | profits 리스트의 0번째 값을 가져와서 시작값으로 삼기 위해서
#  result |정답으로 내보낼 인덱스들을 모으는 변수|  [0]  | 1주차는 무조건 경신한 주라고 했기 때문임(비교할 값이 없기 때문에 그냥 첫 기록으로 인정한다?)



# ============================================================
# [3] 설계 - 무엇을 / 왜
# ------------------------------------------------------------
# ★ 이 문제의 핵심 질문입니다. 반드시 [왜]에 답하세요.
#
#   최고 기록 변수를 profits[0]으로 두고 인덱스 0부터 반복을 돌리면,
#   0주차는 결과에 들어갈까요 안 들어갈까요?
#   [10, 30, 20, 30, 45] 를 넣어 보고 결과를 확인한 뒤,
#   그럼 0주차를 결과에 넣으려면 무엇을 어떻게 해야 하는지 쓰세요.
# ============================================================
# 0. [시작값을 무엇으로 둘 것인가] (변수 2개 모두) high = profits[0] / result = [0]
#    [왜] high변수로 profits 리스트의 첫 번째 값을 꺼내와야한다. 그리고 result에 인덱스를 모아야해서 result = [0] 설정한다.
#
# 1. [반복을 몇 번 인덱스부터 시작할 것인가] 0번 인덱스부터 시작하기
#    [왜] 0번 인덱스부터 꺼내서 반복을 시작한다.
#
# 2. [경신했을 때 무엇을 해야 하는가] (해야 할 일이 2개입니다) 1. high를 갱신 시킨다. 2. 몇번째 인덱스인지 확인하고, result인덱스에 추가해야한다.
#    [왜] 1. 더 큰 값이 나왔기 때문에 기준점을 경신한 값으로 잡아야 한다. 2. 인덱스를 확인하고 result에 계속해서 갱신하는 것이 아닌 값을 추가해야하기 때문이다.
#
# 3. [동점을 경신에서 제외하려면 부등호를 무엇으로 쓸 것인가] >
#    [왜] 같은 값이 아니라 큰 값을 경신해야해서?

#[1트 - 실패]
# def find_record_weeks(profits):
#     high = profits[0]
#     result = [0]

#     for i in range(0, len(profits)): #앞 문제에서 이걸 계속 쓴게 생각이 나서 써봄
#         # for i in profits 이렇게 쓰면, 그냥 []안의 값들만 반복해서 나옴.
#         # 그래서 for i in range(0, len(profits)) -> 이렇게 쓰면 인덱스를 번호를 뽑을 수 있음. 인덱스를 뽑을 때 가장 기본적인 방법으로 쓴다. 앞에 문제들에서 이 부분이해가 너무 안됏엇음,,
#         if i > high :
#             high = i
#             return result.append(i)

#     return result

#[2트]
def find_record_weeks(profits):
    high = profits[0]
    result = [0]

    for i in range(0, len(profits)): 
        if profits[i] > high : #1트에서 틀린 부분임. 그냥 i를 뽑으면 인덱스 번호가 나오는데, 나는 각 인덱스 번호에 할당된 숫자가 나온다고 생각했음 처음에. profits[i]를 할 생각을 못햇다
            high = profits[i]
            result.append(i) #반복이 전부 끝난 뒤에 return을 해야하는데 return과 result를 같이 써버림.;;;

    return result


# ============================================================
# [4] 손 추적표  (실행 전에 작성)
# ------------------------------------------------------------
# 회차 | 인덱스 | 순이익 | 경신?(True/False) | 최고 기록 | 결과 리스트
# 시작 |   -   |   -   |         -     |    10    | [0]
# 1   |   0   |  10   |     False      |    10    | [0]
# 2   |   1   |  30   |      True       |    30    | [0, 1]
# 3   |   2  |  20   |      False      |    30    | [0, 1]
# 4   |   3   |  30   |      False     |    30    | [0, 1]
# 5   |   4  |  45   |      True       |    45    | [0, 1, 4]


# ============================================================
# [5] 직접 넣어본 추가 입력  (실행한 뒤에 작성)
# ------------------------------------------------------------
# 내가 만든 입력          | 그렇게 만든 이유                          | 예상 결과    | 실제 결과
# [10, 10, 20]           | 같은 값이 들어가면 어떻게 되는지    | [0, 2]      | [0, 2]
# [-30, -20, -10, -10]   | 음수에서도 최고 기록 경신이 잘 되는지 | [0, 1, 2]   | [0, 1, 2]


# ============================================================
# [6] 회고
# ============================================================
# 막힌 지점            : i와 profits[i]의 차이를 헷갈렸음 range를 사용하면 i에는 인덱스 번호가 들어오고, 실제 값은 profits[i]로 꺼내야 한다는 점에서 막힌듯
# 문제 소요 시간       : 50분..?
# 예측과 실제가 달랐던 곳 : 처음에는 if i > high 라고 쓰면 각 인덱스에 있는 순이익 값이 비교될 거라고 생각함. 근데 i에는 인덱스 번호가 들어왔고,
# profits[i]를 써야 실제 값이 나온다는 것을 이해햄


# ---- 확인용 출력 (수정하지 마세요) ----
print(find_record_weeks([10, 30, 20, 30, 45]))
print(find_record_weeks([5]))
print(find_record_weeks([9, 8, 7]))
print(find_record_weeks([-10, -20, -5]))
