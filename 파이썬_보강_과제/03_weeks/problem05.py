'''
문제 05 (problem05.py)
------------------------------------------------------------------------
01번, 02번과 같은 성적표를 마지막으로 한 번 더 봅니다.
성적표가 2차원 리스트 scores로 주어질 때
(한 줄이 한 학생이고, 그 줄 안의 값들이 그 학생의 과목별 점수),
아래 두 값을 튜플로 묶어 반환하는 find_top_student_and_subject 함수를
완성하시오.

    (총점이 가장 높은 학생의 번호, 총점이 가장 높은 과목의 번호)

- 학생 수와 과목 수는 서로 다를 수 있습니다.
- 모든 학생은 같은 개수의 과목 점수를 가집니다.
- 학생 번호와 과목 번호는 모두 0부터 시작합니다.
- 총점이 가장 높은 학생이 여러 명이면 번호가 가장 작은 학생을 반환합니다.
  과목도 마찬가지입니다.

예시_01)
    입력: [[1, 2, 3], [4, 5, 6]]
    출력: (1, 2)
          (학생 총점 [6, 15] => 1번 학생 / 과목 총점 [5, 7, 9] => 2번 과목)

예시_02)
    입력: [[50]]
    출력: (0, 0)

예시_03)
    입력: [[3, 1], [1, 3]]
    출력: (0, 0)
          (학생 총점 [4, 4] 동점 / 과목 총점 [4, 4] 동점 => 둘 다 0번)

예시_04)
    입력: [[1, 5], [2, 4], [3, 3]]
    출력: (0, 1)
          (학생 총점 [6, 6, 6] 전부 동점 => 0번 학생
           과목 총점 [6, 12] => 1번 과목)

제한 내장 함수: sum, max, min, index, zip
           => 사용해도 됩니다. 다만 기본적으로 사용하지 말고 구현해보세요.
'''

# ============================================================
# [1] 신호어 찾기  (코드를 치기 전에 작성)
# ============================================================
# 지문에서 찾은 신호어 : 학생 총점, 과목총점, 학생 번호, 과목번호, 동점이면 작은번호, 가장 높은 총점
# 선택한 골격         : (이 문제는 골격이 3개 이상입니다. 모두 쓰세요) 반복문, 조건문, 누적함
# 그렇게 판단한 이유   : 학생별 종점이랑 과목별 총점을 각각 먼저 구하고, 그 총점들 중 가장 큰 값의 위치를 찾아야함
#
# 반환 형태 (지문에서 그대로 옮겨 적기) : 최고 총점 학생의 번호, 최고 총점 과목의 번호
#
# 추가 질문 1: 이 문제는 앞의 01~04번 중 어떤 문제들의 조합인가요? 1번, 2번...?
#             '새로운 유형'이 아니라는 걸 확인하는 질문입니다.
# 답 : 총점 구하고, 과목별 총점 구하고, 인덱스를 찾는다
#
# 추가 질문 2: 예시_04에서 학생 총점은 [6, 6, 6]으로 전부 같습니다.
#             그런데 답이 0번인 이유는 무엇인가요?
#             부등호와 연결해서 한 줄로 적으세요.
# 답 : > 를 사용해서 더 클 때ㅂ만 갱신해야해서 같은 값이면 0번이 그대로 남음


# ============================================================
# [2] 변수 역할표  (코드를 치기 전에 작성)
# ------------------------------------------------------------
# 만들 변수마다 아래 다섯 칸을 채우세요.
# ★ 이 문제는 변수가 많습니다. 하나도 빠뜨리지 말고 전부 적으세요.
#    적고 나서 "이 변수는 마지막에 어디서 읽히는가"를 한 번씩 확인하세요.
#    답이 안 나오는 변수는 만들 필요가 없습니다.
# ============================================================
# 변수 이름          | 담기는 것                    | 시작값      | 시작값의 이유                       | 어느 반복문 안/밖에서 만드는가
# stu_max           | 학생별 총점을 모은 리스트      | []       | 학생 총점을 순서대로 저장해야 해서 | 모든 반복문 밖
# score_max       | 과목별 총점을 모은 리스트      | []       | 과목 총점을 순서대로 저장해야 함  | 모든 반복문 밖
# score             | 현재 학생의 점수 리스트        | 없음     | scores에서 학생 한 명씩 반복문이 꺼내줘야함   | 학생 총점을 구하는 바깥 반복문의 변수
# total         | 현재 학생의 총점              | 0         | 학생 한 명의 점수를 처음부터 누적해야 해서 | 학생 반복문 안, 과목 점수 반복문 밖
# s             | 현재 학생의 한 과목 점수       | 없음        | score에서 점수 하나씩 꺼내려고        | 학생 총점을 구하는 안쪽 반복문의 변수
# c            | 현재 과목 번호                | 0부터 시작   | 과목을 0번부터 순서대로 확인해야 해서     | 과목 총점을 구하는 바깥 반복문의 변수
# total_scores     | 현재 과목의 총점              | 0           | 과목 하나의 점수를 학생별로 처음부터 누적해야 함 | 과목 반복문 안, 학생 반복문 밖
# r               | 현재 학생 번호               | 0부터 시작   | 모든 학생의 현재 과목 점수를 확인  | 과목 총점을 구하는 안쪽 반복문의 변수
# max_student_score | 지금까지 가장 큰 학생 총점     | stu_max[0]  | 처음에는 0번 학생의 총점을 최고값으로 잡는다     | 학생 최고 인덱스를 찾는 반복문 밖
# stu_max_index     | 최고 총점 학생의 번호     | 0      | 처음에는 0번 학생을 최고 학생으로 가정      | 학생 최고 인덱스를 찾는 반복문 밖
# stu_index         | 현재 비교 중인 학생 인덱스     | 1부터 시작   | 0번 학생은 이미 기준으로 잡았으므로 1번부터 비교       | 학생 최고 인덱스를 찾는 반복문의 변수
# max_subject_score | 지금까지 가장 큰 과목 총점     | score_max[0]| 처음에는 0번 과목의 총점을 최고값으로 잡아서  | 과목 최고 인덱스를 찾는 반복문 밖
# score_max_index   | 최고 총점 과목의 번호          | 0           | 처음에는 0번 과목을 최고 과목으로 가정   | 과목 최고 인덱스를 찾는 반복문 밖
# score_index       | 현재 비교 중인 과목 인덱스     | 1부터 시작   | 0번 과목은 이미 기준으로 잡았으므로 1번부터 비교       | 과목 최고 인덱스를 찾는 반복문의 변수


# ============================================================
# [3] 설계 - 무엇을 / 왜
# ------------------------------------------------------------
# ★ 이 문제의 핵심 질문 두 개입니다. 반드시 [왜]에 답하세요.
#
#   (1) 학생 총점과 과목 총점을 '하나의 중첩 반복문' 안에서 동시에
#       구할 수 있을까요? 가능하다면 어떻게, 불가능하다면 왜 그런지
#       본인 말로 적으세요.
#       (정답은 하나가 아닙니다. 본인이 고른 방식과 그 이유를 쓰면 됩니다)
#
#   (2) 최고 총점 변수의 시작값을 0으로 두어도 이 문제에서는 안전합니다.
#       지문의 어느 조건 문장 때문에 안전한가요?
#       그리고 그 조건이 없어진다면 무엇으로 시작해야 하나요?
# ============================================================
# 0-1. [학생 총점 목록 - 시작값과 만드는 위치] stu_max = [] 맨 처음에 만듬
#      [왜] 학생별 총 점수를 하나씩 저장해야해서 빈 리스트로 시작함.
#
# 0-2. [과목 총점 목록 - 시작값과 만드는 위치] score_max = [] 맨 처음에
#      [왜] 과목별 총점을 하나씩 저장해야해서 빈 리스트로 시작한다.
#
# 0-3. [최고 총점 학생 번호 - 시작값] stu_max_index = 0
#      [왜] 처음에 0번 학생을 최고 총점 학생이라고 가정하고, 이후에 더 높은 사람이 나오면 번호 갱신해야댐
#
# 0-4. [최고 총점 과목 번호 - 시작값] score_max_index = 0
#      [왜] 처음에는 -번 과목을 최고 총점수 과목이라고 가정하고 이후에 더 높은 총점의 과목이 나오면 번호 갱신해야댐
#
# 1. [무엇을] stu_max에 저장 학생별 총점을
#    [왜]각 학생의 총점 알아야 함. 최고 총점 학생을 찾으려면
#
# 2. [무엇을] 
#    [왜]
#
# 3. [무엇을]
#    [왜]
#
# 4. [동점일 때 번호가 작은 쪽을 남기려면 부등호를 무엇으로 쓸 것인가] >
#    [왜]  같은 값이면 갱신을 안해야지 먼저 나온 작은 인덱스가 그대로 남음

# 일단 정답을 받을 result = ()을 생성
# 일단 첫번째 for문으로 각 학생들의 점수표를 꺼내야 한다.
# 그리고 각 리스트 점수의 합을 구한다
# 그리고 그 총점 중에서 누구의 점수가 더 큰지 찾는다
# 그리고 큰 값을 반환한다.
# 그런 다음음에 각 과목별로 점수를 더한다.
# 그리고 그 제일 과목이 큰 인덱스를반환
# 이럴려면 각 점수에 인덱스를 부여해야한다.

# #1
# def find_top_student_and_subject(scores):
#     result = ()

#     for score in scores:
#         total = 0
#         for s in score:
#             total = total + s #일단이렇게 하면 총 합을 구할 수가 있음. 근데 더 큰 값을 어떻게 구하지


#2
def find_top_student_and_subject(scores):
    stu_max = [] #학생 총점
    score_max = []  #과목별 총점

    for score in scores:
        total = 0
        for s in score:
            total = total + s 

        stu_max.append(total)  

    for c in range(len(scores[0])): #여기서 한번 삐끗
        total_scores = 0
        for r in range(len(scores)):
            total_scores = total_scores + scores[r][c] # 여기서 한번 막혓습니다 그치만 지피티는 쓰지 않앗더요. 
                                                       # 저는 처음에 행과 열 순서로 꺼낸다기보다 반복문 순서로 꺼내는게 맞다고생각해서 [c][r]로 뒀었음

        score_max.append(total_scores)

    # 여기서 막혔습ㄴ디ㅏ. 제가 생각했던 방식이 있었는데 구현하지 못해서, 원석님이 enumerate사용을 도와주셧으요
    # 학생 총점 리스트 stu_max에서 가장 큰 총점의 학생 번호를 찾아야함
    # max_student_score = stu_max[0] #0번 학생의 총점 6을 현재 최고점이라고 가정한다
    # stu_max_index = 0 # 현재 최고점 학생 번호도 일단 0번이락고 저장한다.
    # for index, stu in enumerate(stu_max): #enumerate()로 인덱스와 값을 같이 꺼낸다
    #     if stu > max_student_score: # 지금보고 있는 학생 총점 stu가 지금까지 최고점보다 큰지 비교
    #         max_student_score = stu # 크면 최고점 바꿈
    #         stu_max_index = index #인덱스도 바꿈


    # max_subject_score = score_max[0]
    # score_max_index = 0

    # for index, subject in enumerate(score_max):
    #     if subject > max_subject_score:
    #         max_subject_score = subject
    #         score_max_index = index

    # return (stu_max_index, score_max_index) # 여기서 둘을 튜블로 반환함

    # 제가 쓰고 싶었던 코드는 enumerate를 안쓰고, 기준 인덱스를 하나 잡아서, 그 다음 인덱스랑 크기 비교를 해서
    # 조건문?반복문으로 작으면 넘기고 큰거면 그 값을 기준인덱스에 넣어서 비교할게 없을 때 까지 돌리는 방법? 을 생각햇습니다

    #정리하자면    
    #처음 값을 기준값으로 잡고, 그다음 값부터 하나씩 비교한다.
    # 현재 값이 기준값보다 작거나 같으면 그대로 넘어가고, 더 크면 그 값을 새로운 기준값으로 바꾼다.
    # 그니까 처음 값을 기준으로 잡고 더 큰 값이 나오면 그걸 새 기준으로 바꾸고 그 바듬 값은 새 기준값이랑 비교한다
    # 이때 그 값의 인덱스도 같이 기억한다.
    # 리스트 끝까지 반복한 뒤 마지막에 남아 있는 기준값의 인덱스를 최고값의 위치로 사용한다.

    #############학생 최대값인덱스

    # max_student_score = stu_max[0]
    # stu_max_index = 0

    # for stu in stu_max :
    #     if stu > stu_max[0]: # 이렇게 하면 맨 처음 값하고만 비교하게된다
    #         max_student_score = stu

    #     else:
    # 약간 이런식으로 하고싶었는데 뭔가 잘못됨

    # # 우리가 하려는 방식은 인덱스 자체를 1,2,3,순서대로 움직이는 반복문이어야됨. 인덱스를 반환해야하기 때문에
    # max_student_score = stu_max[0]
    # stu_max_index = 0

    # for stu_index in range(1, len(stu_max)) :
    #     if stu_max[0] < stu_index : #근데 이렇게 하면 숫자와 인덱스를 비교하게되는데,,
    #         max_student_score = stu_index 
    #         stu_max_index = stu_index

    # #다시
    # max_student_score = stu_max[0]
    # stu_max_index = 0

    # for stu_index in range(1, len(stu_max)) :
    #     if stu_max[0] < stu_max[stu_index]: # 여기가 잘못됨
    #         max_student_score = stu_max[stu_index]
    #         stu_max_index = stu_index

    # #다시 (최종)
    max_student_score = stu_max[0]
    stu_max_index = 0

    for stu_index in range(1, len(stu_max)) :
        if max_student_score < stu_max[stu_index]:
            max_student_score = stu_max[stu_index]
            stu_max_index = stu_index


    ############ 과목총점의 최대값 인덱스 구하기
    max_subject_score = score_max[0]
    score_max_index = 0

    for score_index in range(1, len(score_max)):
        if max_subject_score < score_max[score_index]:
            max_subject_score = score_max[score_index]
            score_max_index = score_index

    return (stu_max_index, score_max_index)




# ============================================================
# [4] 손 추적표  (실행 전에 작성)
# ------------------------------------------------------------
# 입력 [[1, 2, 3], [4, 5, 6]] 기준
# ============================================================

# [1단계] 학생 총점과 과목 총점 구하기
#
# 회차 | 반복 변수 1 | 반복 변수 2 | 꺼낸 점수 | 학생 총점 목록 | 과목 총점 목록
# 시작 |     -      |     -     |     -    |      []       |      []
#
# ---- 학생 총점 구하기 ----
# 1   | [1,2,3]     1         | 1       | []           | []
# 2   | [1,2,3]     2        | 2        | []          | []
# 3   | [1,2,3]    | 3       | 3       | [6]           | []
# 4   |[4,5,6]   | 4         | 4       | [6]          | []
# 5   | [4,5,6]   | 5        | 5      | [6]           | []
# 6   | [4,5,6]   | 6         | 6        | [6,15]       | []
#
# ---- 과목 총점 구하기 ----
# 7    |c=0       | r=0        | 1       | [6,15]        | []
# 8   |c=0        | r=1        | 4       | [6,15]       | [5]
# 9   | c=1       |r=0        | 2       | [6,15]        | [5]
# 10  | c=1      | r=1        | 5       | [6,15]       | [5,7]
# 11  | c=2      | r=0        | 3      | [6,15]        | [5,7]
# 12  | c=2       | r=1        | 6       | [6,15]       | [5,7,9]
#
# [2단계] 두 목록에서 각각 1등 찾기
#
# 대상      | 인덱스 | 꺼낸 총점 | 갱신?(True/False) | 최고 총점 | 최고 번호
# 학생 시작 |   -    |    -     |        -         |    6     |    0
# 학생      |   1    |   15     |       True       |   15     |    1
#
# 과목 시작 |   -    |    -     |        -         |    5     |    0
# 과목      |   1    |    7     |       True       |    7     |    1
# 과목      |   2    |    9     |       True       |    9     |    2

# ============================================================
# [5] 직접 넣어본 추가 입력  (실행한 뒤에 작성)
# ----------------------------------------------------------
# 내가 만든 입력               | 그렇게 만든 이유                                  | 예상 결과 | 실제 결과
# [[5, 1], [4, 2]]          | 학생 총점은 둘 다 6으로 동점이고, 과목 총점은 다르게 | (0, 0)   | (0, 0)
# [[1, 4], [3, 2]]            | 학생 총점은 둘 다 5로 동점이고, 과목 총점은 4와 6으로| (0, 1)   | (0, 1)


# ============================================================
# [6] 회고
# ============================================================
# 막힌 지점            :학생 총점과 과목 총점을 각각 구하는 것까지는 했지만,
#                   과목 총점을 구할 때 scores[r][c]의 행/열 순서를 헷갈렸다.
#                   또 최댓값을 찾을 때 값과 인덱스를 구분하는 부분에서 막혔고,
#                   처음 값인 stu_max[0]과 계속 비교하는 것이 아니라
#                       갱신되는 max_student_score와 비교해야 한다는 점을 이해하는 데 시간이 걸렸다.
# 문제 소요 시간       : 8시간?
# 예측과 실제가 달랐던 곳 :


# ---- 확인용 출력 (수정하지 마세요) ----
print(find_top_student_and_subject([[1, 2, 3], [4, 5, 6]]))#
print(find_top_student_and_subject([[50]]))
print(find_top_student_and_subject([[3, 1], [1, 3]]))
print(find_top_student_and_subject([[1, 5], [2, 4], [3, 3]]))
