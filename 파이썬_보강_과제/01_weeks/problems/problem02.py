'''
문제 02 (problem02.py) - 9점                            
------------------------------------------------------------------------
교육생의 출결을 집계하고 있습니다.
하루하루의 출결 상태가 문자열 리스트로 주어질 때,
'late'(지각)로 기록된 날이 며칠인지 그 개수를 정수로 반환하는
count_late_days 함수를 완성하시오.

- 출결 상태는 'present', 'late', 'absent' 중 하나입니다.
- 기록이 없는 경우 빈 리스트가 주어질 수 있습니다.

예시_01)
    입력: ['present', 'late', 'absent', 'late', 'present']
    출력: 2

예시_02)
    입력: ['present', 'present']
    출력: 0

예시_03)
    입력: []
    출력: 0

제한 내장 함수: count, len
'''

# ============================================================
# [1] 신호어 찾기  (코드를 치기 전에 작성)
# ============================================================
# 지문에서 찾은 신호어 : late로기록된 날이 며칠인지 그 '개수'를
# 선택한 골격         : 누적, 카운팅 
# 그렇게 판단한 이유   : 일단 late로 기록된 날이 몇개인지 개수를 찾아서, 총 누적 개수를 찾아야 하기때문에?
#
# 추가 질문: 1번 문제(누적)와 골격 모양이 거의 같습니다.
#            그런데 무엇이 다른가요? 본인 말로 한 줄 적으세요.
# 답 : 1번 문제는 각 값을 누적해서 더하는 것이었지만, 2번 문제는 late를 뽑아서 누적한다는 점에서 다르다.


# ============================================================
# [2] 설계 - 무엇을 / 왜
# ------------------------------------------------------------
# [왜] 칸에 '그냥', '원래', '강의에서'는 쓸 수 없습니다.
# 1번에서는 값을 '더했는데' 여기서는 왜 1씩만 더하는지 반드시 쓰세요.
# ============================================================
# 1. [무엇을] total = 0을 설정한다.
#    [왜] 처음 late의 개수는 0이기 때문에 0부터 누적 값을 구해야 함 
#         + late가 1개, 2개,3개 이런식으로 값이 누적되어야 해서 1씩 더한다
#
# 2. [무엇을] 조건문을 사용했을 때
#    [왜] if 문을 활용해서, late를 찾아야 하기 때문이다. 만약에 late라면?값을 누적해라. 더해라 이렇게 하기 위해서
#
# 3. [무엇을] count() 써서 late의 개수만 세기.
#    [왜] 꼭 누적을 하지 않아도? list값 중에서 late의 개수만 몇갠지 찾아도 되는거 아닌가? 라는 생각이 들어서

# 아 근데 사실 조건문을 써서 리스트 안의 값이 =="late"면 값을 누적한다. 이렇게 가야할지
# count("late") 이런식으로 개수를 세는 함수를 써서 값을 반환해야할지 감이 안잡혀서 두개로 해보겟습니다..뭐가 맞는지 모르겟어욤

# 조건문 사용했을때
def count_late_days(records):
    total =  0
    for record in records:
        if record == "late":
            total = total + 1
    return(total)

# count() 사용했을때

def count_late_days(records):
    count("late")
#-> 처음에 이렇게 써서 오류발생

# 다시 수정
def count_late_days(records):
    return(records.count("late"))

# ============================================================
# [3] 손 추적표  (실행 전에 작성)
# ------------------------------------------------------------
# 입력 ['present', 'late', 'absent', 'late', 'present'] 기준
# ============================================================
# 회차 | 꺼낸 값 | 조건 판정(True/False) | count
# 1   | present|      False           | 0
# 2   | late   |       True           | 1
# 3   | absent |       False          | 1
# 4   |  late  |       True           | 2
# 5   | present|      False           | 2


# ============================================================
# [4] 회고
# ============================================================
# 막힌 지점            : 조건문을 사용해야하는지 그냥 count함수를 사용해야하는지 감이 안잡혔을 때..뭐가 맞는지 모르겠었었다.
# 30분 규칙 사용       : 예
# 예측과 실제가 달랐던 곳 : 처음에 count()함수 쓴 부분에서 값이 안나와서 당황했음


# ---- 확인용 출력 (수정하지 마세요) ----
print(count_late_days(['present', 'late', 'absent', 'late', 'present']))
print(count_late_days(['present', 'present']))
print(count_late_days([]))
