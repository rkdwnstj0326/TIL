# =============================================================
# [심화] 해시 테이블 (Hash Table)
# - 딕셔너리와 세트가 빠른 이유이자, 순서가 없는 이유
# - 원리: 값(키)을 해시 함수에 넣어 '정수(해시 값)'로 바꾸고,
#         그 정수를 위치(인덱스)로 삼아 저장
#   => 처음부터 하나씩 비교할 필요 없이 한 번에 위치를 계산
#
# =============================================================

# =============================================================
# [1] 정수 세트의 pop 순서 - 작성 순서와 다르게 나온다
# - {3, 2, 1, 9, ...} 순으로 작성했지만 1, 2, 3, 100 ... 순으로 나옴
# - 정수는 해시 값이 자기 자신과 같아서 저장 위치가 값에 따라 정해짐
#   => 그 결과 작은 수부터 나오는 것처럼 보이지만,
#      이는 '정렬된' 것이 아니라 '버킷 위치 순서'일 뿐
# - 마지막 print(my_set)은 10번 모두 꺼냈으므로 set() (빈 세트)
# =============================================================

# 정수
my_set = {3, 2, 1, 9, 100, 4, 87, 39, 10, 52}
print(my_set.pop())
print(my_set.pop())
print(my_set.pop())
print(my_set.pop())
print(my_set.pop())
print(my_set.pop())
print(my_set.pop())
print(my_set.pop())
print(my_set.pop())
print(my_set.pop())
print(my_set)

# =============================================================
# [2] 문자열 세트의 pop 순서 - 실행할 때마다 달라진다
# - 문자열의 해시 값은 파이썬이 시작될 때 정해지는 난수 시드에 따라 달라짐
#   (보안상의 이유로 도입된 '해시 난수화')
# - 그래서 파일을 저장하고 여러 번 실행하면 순서가 매번 바뀜
#   => 정수([1])와 달리 일정한 패턴조차 없음
# - 결론: 세트의 pop 순서는 어떤 경우에도 의존하면 안 됨
# =============================================================

# 문자열
my_str_set = {'a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j'}
# print(my_str_set.pop())
# print(my_str_set.pop())
# print(my_str_set.pop())
# print(my_str_set.pop())
# print(my_str_set.pop())

# -------------------------------------------------------------
# [3] hash() 함수로 직접 확인
# - 정수: 몇 번을 실행해도 hash(1)은 항상 1로 동일
# - 문자열: 프로그램을 새로 실행할 때마다 값이 달라짐
#           (같은 실행 안에서는 동일한 값 유지)
# -------------------------------------------------------------

# print(hash(1))
# print(hash(1))
# print(hash('a'))
# print(hash('a'))

# -------------------------------------------------------------
# [4] hashable - 해시 값을 구할 수 있는 객체
# - 불변(immutable) 객체만 해시 가능 => int, float, str, tuple
# - hash(1)과 hash(1.0)이 같은 이유: 값이 논리적으로 동일하기 때문
# -------------------------------------------------------------

# print(hash(1))
# print(hash(1.0))
# print(hash('1'))
# print(hash((1, 2, 3)))

# -------------------------------------------------------------
# [5] unhashable - 가변 객체는 해시 불가
# - 튜플이라도 '안에 리스트가 들어 있으면' 해시 불가
#   => 내용이 바뀔 수 있으면 해시 값도 바뀌기 때문
# - 왜 막아두었나:
#   저장할 때 계산한 위치와 나중에 찾을 때 계산한 위치가 달라지면
#   데이터를 영영 찾지 못하게 됨 => 해시 테이블의 무결성이 깨짐
# - 그래서 list, set, dict는 dict의 키나 set의 요소가 될 수 없음
# -------------------------------------------------------------

# TypeError: unhashable type: 'list'
# print(hash((1, 2, [3, 4])))

# TypeError: unhashable type: 'list'
# print(hash([1, 2, 3]))
# TypeError: unhashable type: 'list'
# my_set = {[1, 2, 3], 1, 2, 3, 4, 5}
# TypeError: unhashable type: 'set'
# my_dict = {{3, 2}: 'a'}
